hunters:
  service:
    url: http://localhost:8081
server:
  port: 8082
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/insight_db
    username: root
    password: password
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true